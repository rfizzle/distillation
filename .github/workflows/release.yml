# Thin stub — logic lives in rfizzle/concord (mod-release.yml).
# modrinth-id is added via `with:` once the distillation-alchemy-overhaul
# Modrinth project is registered.
name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: rfizzle/concord/.github/workflows/mod-release.yml@master
    with:
      curseforge-id: "1617729"
    secrets: inherit
